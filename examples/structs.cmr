include std->io

struct User {
    name -> str
    sex -> bool
    age -> u8
}

fun main(_argc -> i32, _argv -> str[]) => u8 {
    let user -> User = {
        "Comet",
        false,
        16
    }
    io->out(user.name)
    return 0
}